version: 2
models:
  - name: customer
    columns:
      - name: orderid
        tests:
          - unique:
          - not_null:
      - name: status
        tests:
          - accepted_values:
              values:
                - "delivered"
